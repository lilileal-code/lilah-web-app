Now we will complete the docs/design/tasks.md file based on docs/design/plan.md and docs/design/specification.md. Focus on tasks that update the template application in this workspace so the front end meets the specification. We want a working prototype before any database or backend tasks.

Use docs/design/reference/tasks-guide.md as a guide. Keep the tasks template tables. Adapt the list below. Do not create tasks that rewrite the template from scratch or add login, Supabase, or messaging.

## Task List
- Map template item fields to VisitFile fields
- Load fictional visit data (start with 8, later 18–25)
- Add the demo-data label on the list
- Newest-first cards with title, date, clinician, reason
- Tap a card to open the file
- Detail page in spec section order
- Call-now strip + 911 line
- Bottom bar: Back, Bookmark, Pin, Useful
- Persist flags in the browser
- Filter chips and Active care banner
- Search as-you-type
- Empty search, empty Active care, and broken-file messages
- 375px layout check

Walk the R1–R17 acceptance table

Each task must start with a verb, finish in under a day, and list dependencies.
